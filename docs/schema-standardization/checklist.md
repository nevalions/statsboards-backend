# Checklist

- [ ] Inventory schema files and missing tiers
- [ ] Standardize naming conventions
- [ ] Add `SchemaUpdate` via `make_fields_optional()`
- [ ] Add `model_config = ConfigDict(from_attributes=True)` to output schemas
- [ ] Add missing `WithDetails` and `WithFullDetails` schemas
- [ ] Update routers to use consistent response schemas
- [ ] Update tests and docs
