version: '3'

networks:
  custom_bridge:
    driver: bridge

services:
  statsboards-backend:
    build:
      context: .
      dockerfile: Dockerfile.prod
    command: poetry run python run_prod_server.py
    ports:
      - "9000:9000"
    env_file:
      - .env.prod
    networks:
      - custom_bridge