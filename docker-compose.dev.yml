version: '3'
services:
  statsboards-backend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    command: poetry run python runserver.py
    ports:
      - "9000:9000"
    env_file:
      - .env.dev